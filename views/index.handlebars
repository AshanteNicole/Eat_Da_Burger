<h1>Eat da Burger</h1>




<h2>Ready to eat</h2>
{{#each data}}
  {{#if devoured}}
  {{else}}
    <p>{{burger_name}} <button data-id="{{id}}" class="devour">devour it</button></p>
  {{/if}}
{{/each}}



<h2>Devoured! Yummy!</h2>

{{#each data}}
  {{#if devoured}}
  <p>{{burger_name}} | {{id}} | {{devoured}}</p>
  {{else}}
  {{/if}}
{{/each}}


<script>

  $('.devour').on('click', function(){
    var id = $(this).attr('data-id')
    console.log(id);

    // make a PUT or PATCH request to your sever with id
    // when it comes back then location.reload()
  })

</script>
